<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/sns"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".checkinf.InterestActivity" />
        <activity android:name=".others.AddFriendOrGroupActivity" />

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/network_security_config" />

        <activity android:name=".checkinf.ModifyData" />
        <activity android:name=".moment.MyMomentActivity" />
        <activity android:name=".friendlist.FriendListActivity" />
        <activity android:name=".checkinf.FriendInfActivity" />
        <activity android:name=".checkinf.GroupInfActivity" />
        <activity android:name=".checkinf.MineActivity" />
        <activity android:name=".friendlist.GroupMembersList" />
        <activity android:name=".chatlist.ChatListActivity" />
        <activity android:name=".chat.ChatActivity" />
        <activity android:name=".others.MainActivity" />
        <activity android:name=".others.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>